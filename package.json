{
  "name": "staker",
  "license": "UNLICENSED",
  "scripts": {
    "eth-test": "SCOPE=test BLOCKCHAIN=ethereum NETWORK=mainnet yarn prepare:subgraph && yarn deploy:mainnet:test",
    "eth-prod": "SCOPE=prod BLOCKCHAIN=ethereum NETWORK=mainnet yarn prepare:subgraph && yarn deploy:mainnet:prod",
    "prepare:subgraph": "mustache ./config/${NETWORK}-${SCOPE}.json ./templates/${BLOCKCHAIN}.subgraph.template.yaml > subgraph.yaml && rm -rf generated && yarn generate:schema && graph codegen && graph build",
    "generate:schema": "cp ./schemas/${BLOCKCHAIN}.schema.graphql schema.graphql",
    "deploy:mainnet:test": "graph deploy --node https://api.thegraph.com/deploy/ sjuanati/Staker",
    "deploy:mainnet:prod": "graph deploy --node https://api.thegraph.com/deploy/ sjuanati/gro-test-sjs",
    "deploy:avalanche:prod": "graph deploy --node https://api.thegraph.com/deploy/ sjuanati/gro-test-avax",
    "create-local": "graph create --node http://localhost:8020/ sjuanati/Staker",
    "remove-local": "graph remove --node http://localhost:8020/ sjuanati/Staker",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sjuanati/Staker",
    "codegen": "graph codegen",
    "build": "graph build"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.31.0",
    "@graphprotocol/graph-ts": "0.27.0",
    "mustache": "^4.2.0"
  }
}
